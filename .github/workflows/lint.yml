name: Lint
on:
  # pull_request:
  #   branches:
  #     - '!main'
  workflow_dispatch:
jobs:
  eslint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Lint
        run: ./node_modules/.bin/eslint .
